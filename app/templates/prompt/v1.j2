Create a shot prompt for shot {{ shot_index }}.
Style preset: "{{ style_preset }}"
Scene: "{{ scene }}"
Subject: "{{ subject }}"
Action: "{{ action }}"
Camera: "{{ camera }}"
Lighting: "{{ lighting }}"
Emotion: "{{ emotion }}"
Return positive and negative prompts.
